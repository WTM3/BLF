#!/bin/bash

# Start the MCP server using the @smithery/cli for optimal AI agent integration
npx -y @smithery/cli run @block/code-mcp --port 9876

# This script provides the narrow bridge between chaos and control
# by standardizing the server startup process 